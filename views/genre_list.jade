extends layout

block content
  h1= title

  if genre_list.length
    ul
      each author in genre_list
        li
          a(href=author.url) #{author.name}
          |  (#{author.date_of_birth} - #{author.date_of_death})
  else
    p There are no authors.
